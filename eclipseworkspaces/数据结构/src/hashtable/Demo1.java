package hashtable;

public class Demo1 {
	public static void main(String[] args) {
	/**
	 * 哈希表的插入和查找是很优秀的
	 * 可是当哈希表接近装满时，因为数组的扩容问题，性能较低，(转移到更大的哈希表中).
	 * 加载因子 0.75  
	 * 数组是会记录添加的顺序的，按照索引位置来存储的，允许元素重复
	 * 哈希表中：元素是不能重复的，对象如果相同，hashcode相同，-> index相同
	 * 不会记录
	 * 
	 * 
	 */
	}
}
